services:
  llm-server:
    image: ghcr.io/ggml-org/llama.cpp:server-vulkan@sha256:1f834a5e0248d83c4cc459591a53250499592742f3c33f8830054e9ccb1c4c5b
    restart: unless-stopped
    ports:
      - "9001:8080"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 10
      start_period: 10m
    volumes:
      - llm_cache:/root/.cache
    devices:
      - /dev/dri:/dev/dri
    environment:
      - GGML_VK_VISIBLE_DEVICES=0
      - HF_TOKEN=${HF_TOKEN:-}
    command:
      - "--hf-repo"
      - "${HF_REPO}"
      - "--hf-file"
      - "${HF_FILE}"
      - "--port"
      - "8080"
      - "--ctx-size"
      - "8192"
      - "--n-gpu-layers"
      - "all"
      - "--threads"
      - "8"
      - "--threads-batch"
      - "16"
      - "--parallel"
      - "1"
      - "--n-predict"
      - "512"
      - "--reasoning"
      - "off"
      - "--metrics"

volumes:
  llm_cache:
